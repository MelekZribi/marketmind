register :
<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<title>Inscription - MarketMind</title>
<style>
  :root {
    --bg: #0B0F14; --panel: #121821; --border: #232B36;
    --text: #E6ECF3; --muted: #7C8899; --accent: #3DDC97;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0; background: var(--bg); color: var(--text);
    font-family: 'Segoe UI', system-ui, sans-serif;
    display: flex; align-items: center; justify-content: center;
    min-height: 100vh;
  }
  .card {
    width: 100%; max-width: 380px; background: var(--panel);
    border: 1px solid var(--border); border-radius: 12px; padding: 32px;
  }
  h1 { font-size: 20px; margin: 0 0 4px 0; }
  .accent { color: var(--accent); }
  p.sub { color: var(--muted); font-size: 13px; margin: 0 0 24px 0; }
  label { font-size: 13px; color: var(--muted); display: block; margin-bottom: 6px; }
  input {
    width: 100%; background: #0B0F14; border: 1px solid var(--border);
    color: var(--text); padding: 11px 12px; border-radius: 8px;
    font-size: 14px; margin-bottom: 16px; outline: none;
  }
  input:focus { border-color: var(--accent); }
  button {
    width: 100%; background: var(--accent); color: #06110C; border: none;
    padding: 12px; border-radius: 8px; font-weight: 600; cursor: pointer; font-size: 14px;
  }
  .flash { background: #3a1c1c; color: #ffb3b3; padding: 10px 12px; border-radius: 8px; font-size: 13px; margin-bottom: 16px; }
  .footer-link { text-align: center; margin-top: 18px; font-size: 13px; color: var(--muted); }
  .footer-link a { color: var(--accent); text-decoration: none; }
</style>
</head>
<body>
<div class="card">
  <h1>Market<span class="accent">Mind</span></h1>
  <p class="sub">Cree un compte pour sauvegarder ton historique d'analyses.</p>

  {% with messages = get_flashed_messages() %}
    {% if messages %}
      {% for m in messages %}<div class="flash">{{ m }}</div>{% endfor %}
    {% endif %}
  {% endwith %}

  <form method="POST">
    <label>Nom d'utilisateur</label>
    <input type="text" name="username" required autofocus>
    <label>Mot de passe</label>
    <input type="password" name="password" required>
    <button type="submit">Créer mon compte</button>
  </form>

  <div class="footer-link">Déjà un compte ? <a href="{{ url_for('auth.login') }}">Connecte-toi</a></div>
</div>
</body>
</html>
